import React from 'react';
import styles from './index.scss';

const Input = () => (
  <input className={styles.input} type="text" placeholder="Search Plate, Model, Status and more..." />
);

export default Input;
