import React from 'react';

const NotFoundComponent = () => (
  <div >
    <h1 style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100%',
      }}
    >
        .404 Nothing found component
    </h1>
  </div>
);

export default NotFoundComponent;

